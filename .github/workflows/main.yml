name: Changelog Versioning Workflow

on:
  push:
    branches:
      - main

jobs:
  create_branch:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3
        
        
    - name: Create new branch
      # run: current_date=$(date +"%Y-%m-%d") && echo $current_date && git branch $current_date && git checkout $current_date &&  git push -u origin $current_date
      run: bash versioning-workflow.sh
